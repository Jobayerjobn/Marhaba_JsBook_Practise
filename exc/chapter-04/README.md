# Chapter-04 || Comparison Operators

## Practice problems
- How to use Comparison operators
- Practice Example, greater than (>), less than (<), equal (==), not equal(!=), AND (&&), OR (||), strict equal (===), less than or equal (<=), greater than equal (>=), not equal (!=), not strict equal (!==) .


// make user admin 
const userLevel = 4;

const price = 50;
//price = 100;
console.log(price);
//output: 50

// The number of items in the shopping cart.,
// The v value is initially set to 0 because the user hasn't added any items yet.
//It will increase whenever the user adds an items to the cart.

let totalItems = 0;

/* 
The variable stores the maximum number of attempts of user is allowed to make while logging into their account.It is set to 3. If the user exceeds their limit,their account will be temporarily locked.
*/

let maxLevelAttempts = 3;
